# Terraform local working directory
**/.terraform/

# Terraform state files (never commit)
**/*.tfstate
**/*.tfstate.*
**/terraform.tfstate.d/

# Terraform lock file (choose one approach)
# If you want to COMMIT lock files, comment this line out:
**/.terraform.lock.hcl

# Terraform tfvars files (often contain env-specific values)
**/*.tfvars
**/*.tfvars.json
!**/*.tfvars.example
!**/*.example.tfvars

# Crash logs
**/crash.log
**/crash.*.log

# OS / IDE
.vscode/
.DS_Store
Thumbs.db
nul
# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/.terraform.lock.hcl

# Generated policy outputs (optional: keep it, but do not keep multiple copies)
# infra/terraform/envs/**/policy.auto.tfvars.json

# Evidence outputs (keep, but avoid binaries)
docs/evidence/**/.terraform/
docs/evidence/**/*.zip
docs/evidence/**/*.exe
docs/evidence/**/*.bin

# OS noise
Thumbs.db
.DS_Store

# Generated policy inputs
infra/terraform/envs/*/policy.auto.tfvars.json
